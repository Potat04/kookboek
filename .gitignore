*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

*.salive

# Signing. Never commit these — whoever has them can ship updates as you.
keystore.properties
*.jks
*.keystore

# Agent config: the shared settings and knowledge base are committed on purpose,
# personal overrides are not.
.claude/settings.local.json

app/release
# Skills borrowed from elsewhere, kept out of the repo while we try them out.
# Our own skills are listed below and stay committed.
/.claude/skills/*
/.claude/worktrees/
!/.claude/skills/smoke-test/
!/.claude/skills/feature-review/
